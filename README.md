# Discordified-MediaMTX
MediaMTX deployment with additional Discord webhook scripts. Every time a stream starts, it auto-generates new credentials to access the player and automatically embeds them into a Discord link.
